docker最新镜像源

​                          [地址①:dockerpull.com](https://dockerpull.com/)

​                          [地址②:dockerproxy.cn](https://dockerproxy.cn/)

## 使用方法1

**假如拉取原始镜像命令如下**

```sh
docker pull whyour/qinglong:latest                                                                                                                                         
```

**仅需在原命令前缀加入加速镜像地址 例如：**

```sh                                              
docker pull dockerpull.com/whyour/qinglong:latest
```



## 使用方法②

**一键设置镜像加速：**修改文件 `/etc/docker/daemon.json`（如果不存在则创建）

```sh
/etc/docker/daemon.json
```

**修改JSON文件 更改为以下内容 然后保存**

```sh
{"registry-mirrors": ["https://dockerpull.com"]}
```

**保存好之后 执行以下两条命令**

```sh
sudo systemctl daemon-reload #重载systemd管理守护进程配置文件
sudo systemctl restart docker #重启 Docker 服务
```

© dockerpull.com. All rights reserved.

​                                  今日访问人数**258**总访问量**28,794**