# 我的轩辕docker镜像网站的相关信息

## 1. 专属docker镜像下载地址：

```sh
lyb4mjb03lzrxc.xuanyuan.run
```

## 2. 我的账号密码：

```sh
账号：17520486630
密码：9tuvQs&gI2xSUB+$
```

## 3. 下载基于arm的ubuntu镜像：

```sh
docker pull --platform=linux/arm64/v8 lyb4mjb03lzrxc.xuanyuan.run/ubuntu
```

下载来的镜像为ubuntu24.04 aarch64的镜像。此时需要修改源：

## 4. 修改ubuntu24.04为国内源：

### 4.1. 因为24.04的源已经采用了新的方式，在修改之前，需要先安装：

```sh
apt install ca-certificates
```

为了能够编辑源文件，还需要安装：

```sh
apt install nano
```

### 4.2. 编辑源文件：

```sh
nano /etc/apt/sources.list.d/ubuntu.sources
```

其内容为（采用国内清华的镜像网站）：

```sh
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports 
Suites: noble noble-updates noble-backports noble-security 
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
```

然后更新源：

```sh
apt update
```

## 5. 安装以下软件：

安装gcc编译器：

```sh
apt install build-essential
```

安装cmake：

```sh
apt install cmake
```

